import { RoutePageLayoutProps } from '.'

const RoutePageLayout = ({ children }: RoutePageLayoutProps) => {
  return <div className="route-page-layout">{children}</div>
}

export default RoutePageLayout
